# ============================================================
# Scholar Scraper — Pinned Dependencies
# ============================================================
# IMPORTANT: free-proxy MUST stay at 1.0.6
# free-proxy >= 1.1.0 changed get_proxy_list() to require a
# mandatory `repeat` argument, breaking scholarly 1.7.11.
# DO NOT upgrade free-proxy without testing first.
# ============================================================

scholarly==1.7.11
free_proxy==1.0.6      # PINNED — do not upgrade
stem==1.8.2
httpx==0.27.2
requests==2.34.2
flask>=2.0
